# Arjun Thapa — Portfolio

A dark-themed React + Tailwind CSS portfolio website.

## Setup

```bash
npm install
npm run dev
```

Then open http://localhost:5173

## Build

```bash
npm run build
```

Output will be in the `dist/` folder — deploy anywhere (Vercel, Netlify, GitHub Pages, etc.)
